export type Lvl = "built" | "integrated" | "conceptual";
export interface Concept { t: string; say: string; lvl: Lvl; fu?: string }
const c = (t: string, say: string, lvl: Lvl = "conceptual", fu?: string): Concept => ({ t, say, lvl, fu });

export const JAVA: Concept[] = [
  c("OOP: four pillars", "Basically: encapsulation hides state behind methods, inheritance reuses behaviour, polymorphism lets different types respond to the same call, and abstraction exposes what matters. I prefer composition over inheritance in most cases.", "conceptual", "Composition vs inheritance?"),
  c("SOLID", "Single responsibility, open for extension but closed for modification, Liskov substitution, interface segregation, and dependency inversion. In my Rule Builder, field renderers follow open/closed: a new field type is a new component, not a change to the wizard.", "built"),
  c("Interface vs abstract class", "An interface defines a contract; since Java 8 it can have default methods. An abstract class can hold state and partial implementation. I use interfaces for capabilities and abstract classes when subclasses share real code.", "conceptual"),
  c("equals vs ==", "== compares references for objects, equals compares logical equality when it's overridden. If I override equals, I must override hashCode too, or hash-based collections break.", "conceptual"),
  c("HashMap internals", "It uses hashCode to pick a bucket, then equals to find the key inside it. Collisions go into a list or, when long, a tree. It resizes when the load factor is exceeded. It isn't thread-safe; use ConcurrentHashMap for concurrency.", "conceptual", "Why must key be immutable?"),
  c("ArrayList vs LinkedList", "ArrayList is backed by an array, so random access is fast and appends are amortised constant. LinkedList is quick at inserting in the middle by node, but slow to reach an index. In practice ArrayList wins almost always.", "conceptual"),
  c("HashSet", "It's a HashMap under the hood, storing elements as keys. No duplicates, no guaranteed order; LinkedHashSet keeps insertion order and TreeSet keeps sorted order.", "conceptual"),
  c("Concurrency basics", "Threads share memory, so you need synchronisation. I'd use higher-level tools: ExecutorService, CompletableFuture, ConcurrentHashMap, and immutable objects. Watch for race conditions, deadlocks and visibility issues, which volatile and locks address.", "conceptual", "synchronized vs volatile?"),
  c("Checked vs unchecked exceptions", "Checked exceptions must be handled or declared and suit recoverable conditions. Unchecked ones extend RuntimeException and represent programming errors. Spring's @Transactional rolls back on unchecked by default.", "conceptual"),
  c("JVM & garbage collection", "The JVM runs bytecode and manages memory in heap and stack. The garbage collector frees objects that are no longer reachable. Modern collectors like G1 balance throughput and pause time. Memory leaks are usually lingering references.", "conceptual", "What causes OutOfMemoryError?"),
  c("String & immutability", "Strings are immutable, so they're thread-safe and can be cached in the string pool. For heavy concatenation in loops, I'd use StringBuilder. Immutable objects are easier to reason about and share safely.", "conceptual"),
  c("Streams & Optional", "Streams let you express transformations declaratively: filter, map, collect. Optional represents 'maybe absent' values so I avoid null checks. I don't use Optional for fields or parameters.", "conceptual"),
  c("Generics", "They give compile-time type safety for collections and APIs. Type erasure means the type info isn't available at runtime. Wildcards like ? extends T handle read-only cases.", "conceptual"),
  c("Records & modern Java", "Records are concise immutable data carriers. Java 21 also adds virtual threads, which make blocking code scale better. I'd bring these into a modern service.", "conceptual"),
];
export const SPRING: Concept[] = [
  c("Dependency injection / IoC", "Instead of a class creating its dependencies, the container creates and injects them. Constructor injection makes dependencies explicit and testable. It's inversion of control: the framework controls the object lifecycle.", "conceptual"),
  c("Controller–Service–Repository", "The controller handles HTTP and validation, the service holds business logic and transactions, and the repository handles persistence. It keeps responsibilities separate and testable.", "integrated"),
  c("REST controllers", "@RestController with @GetMapping, @PostMapping and @RequestBody. I return ResponseEntity with proper status codes, and use DTOs so I don't expose entities directly.", "conceptual", "Why DTOs?"),
  c("Validation & exception handling", "Bean Validation annotations like @Valid and @NotNull on request DTOs, and a @ControllerAdvice to turn exceptions into a consistent error response.", "conceptual"),
  c("Spring Security & JWT", "A filter validates the token and sets the SecurityContext; then method or URL rules check roles. I've consumed JWT-based login as a client and built the pattern in a personal project.", "conceptual", "Stateless vs session?"),
  c("JPA / Hibernate", "JPA maps entities to tables. Watch lazy loading and the N+1 problem; use fetch joins or entity graphs. Keep transactions short.", "conceptual", "What's N+1 and the fix?"),
  c("@Transactional", "It works through a proxy: a method runs in a transaction and rolls back on unchecked exceptions. Self-invocation bypasses the proxy, and propagation and isolation can be configured.", "conceptual"),
  c("Connection pooling", "Use HikariCP with a sized pool. Too many connections overwhelm the database; too few cause waiting. Set timeouts, and monitor pool usage.", "conceptual"),
  c("Profiles & configuration", "application-{profile}.yml and environment variables for per-environment configuration. Secrets come from a secrets manager, not from the repo.", "conceptual"),
  c("Logging", "SLF4J with Logback or Log4j2, structured logs with a correlation ID, and levels used sensibly. Never log secrets or personal data.", "integrated"),
  c("Testing", "Unit tests with JUnit and Mockito; slice tests with @WebMvcTest or @DataJpaTest; and integration tests with Testcontainers.", "conceptual"),
  c("Microservices with Spring", "Small services with their own data, communicating over REST or messaging, with resilience patterns like timeouts, retries and circuit breakers. Don't start there unless the team needs it.", "conceptual"),
  c("Actuator & observability", "Actuator gives health and metrics endpoints; Micrometer exports metrics to Prometheus or CloudWatch. Health checks drive readiness and liveness.", "conceptual"),
];
export interface Svc { name: string; what: string; why: string; how: string; alt: string; trade: string; scale: string; cost: string; sec: string; fail: string; mon: string; fu: string; lvl: Lvl }
export const AWS: Svc[] = [
  { name: "S3", lvl: "integrated", what: "Object storage.", why: "Cheap, durable storage for events, Parquet, installers and static assets.", how: "Raw events, data lake, agent packages, static app; presigned URLs for download.", alt: "EFS/EBS (filesystems/blocks), a database for structured data.", trade: "Not a file system; consistency model for listing; request costs.", scale: "Practically unlimited; use key prefixes and multipart.", cost: "Storage tiers + requests; lifecycle rules.", sec: "Block public access, bucket policies, SSE, presigned URLs.", fail: "Accidental public exposure; deleted objects without versioning.", mon: "S3 metrics, access logs, CloudTrail data events.", fu: "How do presigned URLs work and what if one leaks?" },
  { name: "CloudFront", lvl: "integrated", what: "CDN.", why: "Edge caching, HTTPS, custom domain in front of a private bucket.", how: "Serves the React app from S3; invalidation on deploy.", alt: "Serving directly from S3 or Nginx on EC2.", trade: "Invalidation and cache TTL discipline.", scale: "Global edge scale.", cost: "Data transfer + requests; cheap for static sites.", sec: "OAC for private S3, WAF, TLS.", fail: "Stale content after deploy.", mon: "Cache hit ratio, 4xx/5xx.", fu: "How do you version assets?" },
  { name: "Lambda", lvl: "integrated", what: "Function-as-a-service.", why: "Bursty, event-driven work without servers.", how: "Resolvers, workers, package builders (owned by backend).", alt: "ECS/Fargate, EC2.", trade: "Cold starts, 15-min limit, concurrency limits.", scale: "Per-request concurrency; reserved/provisioned concurrency.", cost: "Per invocation + GB-seconds.", sec: "Least-privilege execution roles; VPC only when needed.", fail: "Timeouts, throttling, retries duplicating work.", mon: "Errors, duration p95, throttles, concurrent executions, DLQ.", fu: "How do you make Lambda handlers idempotent?" },
  { name: "API Gateway", lvl: "integrated", what: "Managed API front door.", why: "Auth, throttling and Lambda integration for agent endpoints (mTLS + custom authorizer).", how: "Agent APIs for enrolment/heartbeat/config.", alt: "ALB, AppSync.", trade: "Cost and latency vs ALB; payload limits.", scale: "Managed, with throttling limits.", cost: "Per request.", sec: "Authorizers, mTLS, WAF, usage plans.", fail: "Throttling, integration timeouts (29s).", mon: "4xx/5xx, latency, count.", fu: "REST vs HTTP API?" },
  { name: "DynamoDB", lvl: "integrated", what: "Managed NoSQL key-value/document store.", why: "Fast key lookups at scale with no servers.", how: "Single-table with org-prefixed keys (backend-owned).", alt: "PostgreSQL/RDS.", trade: "No joins; access-pattern-first modelling; hot partitions.", scale: "Partitioned horizontally; on-demand or provisioned.", cost: "Per read/write unit + storage.", sec: "IAM, KMS encryption, fine-grained access.", fail: "Throttling on hot keys; GSI lag.", mon: "Throttled requests, consumed capacity, latency.", fu: "What are GSI and LSI?" },
  { name: "AppSync", lvl: "built", what: "Managed GraphQL.", why: "Queries, mutations and subscriptions without running a server.", how: "Console operations and the async result subscription.", alt: "Apollo Server on ECS, REST.", trade: "Resolver limits, lock-in, debugging.", scale: "Managed; watch connection limits.", cost: "Per query + real-time connection minutes.", sec: "Lambda authorizer/IAM/Cognito/OIDC auth modes; field-level checks.", fail: "Resolver errors returned as 200 with errors.", mon: "Latency, 4xx/5xx, subscription errors.", fu: "How does auth work on subscriptions?" },
  { name: "SNS", lvl: "integrated", what: "Pub/sub messaging.", why: "Decouple fast responses from slow work.", how: "startEdrAsyncQuery and package builds publish to topics.", alt: "SQS, EventBridge, Kafka.", trade: "No replay; at-least-once; fan-out only.", scale: "Managed high throughput.", cost: "Per publish/delivery.", sec: "Topic policies, encryption.", fail: "Delivery failures → DLQ needed.", mon: "NumberOfNotificationsFailed, DLQ depth.", fu: "SNS vs SQS vs EventBridge?" },
  { name: "Kinesis / Firehose", lvl: "integrated", what: "Streaming ingest and delivery.", why: "Deliver telemetry into S3 without managing consumers.", how: "Agents → Firehose → gzipped JSON in S3.", alt: "Kinesis Data Streams, Kafka/MSK.", trade: "Firehose = managed delivery, buffered (not real-time); Streams = replay/consumers, shard management.", scale: "Auto-scales (Firehose).", cost: "Per GB ingested.", sec: "IAM, encryption.", fail: "Delivery to S3 failures → error prefix.", mon: "DeliveryToS3.Success, IncomingBytes.", fu: "Streams vs Firehose?" },
  { name: "IAM", lvl: "integrated", what: "Identity & permissions.", why: "Least privilege for functions and services.", how: "Roles per Lambda; IAM Roles Anywhere for agents (backend-owned).", alt: "n/a", trade: "Policy complexity.", scale: "n/a", cost: "Free.", sec: "Least privilege, no wildcards, MFA, role assumption.", fail: "Over-broad policies.", mon: "CloudTrail, Access Analyzer.", fu: "Role vs user vs policy?" },
  { name: "CloudWatch", lvl: "integrated", what: "Logs, metrics, alarms.", why: "Observe Lambda and services.", how: "Logs and dashboards; SLO tracking.", alt: "Datadog, New Relic, Prometheus/Grafana.", trade: "Costly at volume; basic UX.", scale: "Managed.", cost: "Per GB logs + metrics + alarms.", sec: "Access control on log groups.", fail: "Alarm noise.", mon: "n/a", fu: "What alerts would you set on a Lambda?" },
  { name: "ECS", lvl: "conceptual", what: "Container orchestration.", why: "Run containers without managing Kubernetes.", how: "Not used by me.", alt: "EKS, Lambda.", trade: "More ops than Lambda, less than K8s.", scale: "Service auto scaling.", cost: "Fargate vCPU/GB or EC2.", sec: "Task roles, security groups.", fail: "Task OOM, bad health checks.", mon: "CPU/memory, task count.", fu: "Fargate vs EC2 launch type?" },
  { name: "ECR", lvl: "conceptual", what: "Container image registry.", why: "Store/scan images.", how: "Not used directly.", alt: "Docker Hub, GHCR.", trade: "Vendor tie.", scale: "Managed.", cost: "Storage.", sec: "Image scanning, IAM.", fail: "Stale images.", mon: "Scan findings.", fu: "How do you tag images?" },
  { name: "EC2", lvl: "conceptual", what: "Virtual servers.", why: "Full control.", how: "Not used directly.", alt: "Lambda, ECS.", trade: "Patching and scaling burden.", scale: "Auto Scaling Groups.", cost: "Per hour; reserved/spot.", sec: "Security groups, patching, SSM.", fail: "Instance failure without ASG.", mon: "CPU, status checks.", fu: "When would you choose EC2?" },
  { name: "RDS", lvl: "conceptual", what: "Managed relational DB.", why: "SQL with backups, Multi-AZ.", how: "Not used by me in prod (SQL concepts yes).", alt: "DynamoDB, Aurora.", trade: "Vertical scaling limits; connections.", scale: "Read replicas, Aurora.", cost: "Instance hours + storage.", sec: "VPC, KMS, IAM auth.", fail: "Failover, connection storms.", mon: "CPU, connections, replica lag.", fu: "Lambda + RDS connections?" },
  { name: "VPC", lvl: "conceptual", what: "Private network.", why: "Isolate resources.", how: "Some Lambdas run in a VPC to reach private stores (backend-owned).", alt: "n/a", trade: "NAT costs; ENI/cold-start history.", scale: "Subnets across AZs.", cost: "NAT gateways.", sec: "SGs, NACLs, endpoints.", fail: "Misrouted subnets.", mon: "Flow logs.", fu: "Public vs private subnet?" },
  { name: "CloudFormation", lvl: "conceptual", what: "AWS IaC templates.", why: "Repeatable environments.", how: "Understand via CDK.", alt: "Terraform, CDK.", trade: "Verbose JSON/YAML; stack limits.", scale: "n/a", cost: "Free.", sec: "Least-privilege deploy roles.", fail: "Drift, failed rollbacks.", mon: "Stack events, drift detection.", fu: "CloudFormation vs Terraform?" },
  { name: "Terraform", lvl: "conceptual", what: "Multi-cloud IaC.", why: "Portable IaC with state.", how: "Not used by me.", alt: "CDK/CloudFormation.", trade: "State management/locking.", scale: "n/a", cost: "OSS/Cloud.", sec: "Secrets in state.", fail: "State corruption/drift.", mon: "Plan diffs in CI.", fu: "How do you manage state safely?" },
];
export const FRONTEND: Concept[] = [
  c("Hooks basics", "useState for local state, useEffect for side effects with cleanup, useMemo and useCallback for stable values, useRef for mutable values that don't re-render. Rules: call at the top level only.", "built"),
  c("useEffect pitfalls", "Missing dependencies cause stale closures; missing cleanup causes leaks like duplicate subscriptions. For data fetching I prefer a data library over raw effects.", "built"),
  c("useMemo / useCallback / React.memo", "They avoid unnecessary work or re-renders, but only help when profiling shows a problem and props are stable. Overusing them adds complexity.", "built"),
  c("Component architecture", "Small presentational components, container hooks for data, feature folders. In Rule Builder, steps are components, and field renderers are chosen by schema.", "built"),
  c("State management", "Server state in a data library (Apollo cache), UI state local, cross-cutting state in Context. Redux only if the state is truly global and complex.", "built"),
  c("Performance", "Measure first. Code splitting, virtualization, memoization where profiling shows churn, and server-side pagination.", "built"),
  c("Virtualization", "Render only visible rows plus a buffer. Trade-off: accessibility and find-in-page.", "built"),
  c("Forms & validation", "Controlled inputs, schema-based validation, step gating. Server remains authority.", "built"),
  c("Accessibility", "Semantic HTML, labels, keyboard navigation, focus management, contrast, ARIA only when needed. Test with a screen reader and axe.", "built"),
  c("Responsive design", "Mobile-first CSS, flexible grids, relative units, and testing at real breakpoints.", "built"),
  c("Browser rendering", "Parse → DOM/CSSOM → layout → paint → composite. Avoid layout thrash; prefer transform/opacity for animations.", "conceptual"),
  c("Virtual DOM & reconciliation", "React diffs the previous and new element trees using keys and type, then commits minimal DOM changes. Keys matter for lists.", "conceptual"),
  c("Lazy loading / code splitting", "React.lazy with Suspense per route; dynamic import for heavy libraries.", "built"),
  c("Error boundaries", "Class-based components that catch render errors in children and show a fallback; pair with error reporting.", "conceptual"),
  c("WebSockets / subscriptions in React", "Subscribe in an effect, clean up on unmount, and merge updates into state or cache; handle reconnects.", "built"),
  c("Testing", "React Testing Library for behaviour, unit tests for pure logic, and a few end-to-end tests.", "built"),
  c("React vs Angular", "Angular is opinionated: modules, DI, RxJS, CLI. React is a library with choices. Both use components and TypeScript. My plan for Angular is to map concepts and pair.", "conceptual"),
  c("Front-end observability", "Real-user monitoring: Core Web Vitals, JavaScript error reporting, API timing and correlation IDs to link with backend traces.", "conceptual"),
];
export const DB: Concept[] = [
  c("Joins", "INNER returns matches, LEFT keeps all from the left, and so on. Use indexes on join keys.", "integrated"),
  c("Indexes", "B-tree indexes speed lookups and sorting at the cost of writes and storage. Composite index order matters.", "integrated", "Covering index?"),
  c("Transactions & ACID", "Atomicity, consistency, isolation, durability. Use transactions for multi-step invariants.", "conceptual"),
  c("Isolation levels & locking", "Read committed, repeatable read, serializable; higher isolation reduces anomalies but increases contention. Optimistic vs pessimistic locking.", "conceptual"),
  c("Normalisation vs denormalisation", "Normalise to avoid duplication; denormalise deliberately for read performance.", "integrated"),
  c("Query optimisation", "EXPLAIN, indexes, selective columns, avoid N+1, keyset pagination.", "integrated"),
  c("Pagination", "Offset is simple but slow at depth; keyset/cursor pagination is stable and fast.", "integrated"),
  c("DynamoDB keys", "Partition key distributes data; sort key orders within it. Design keys around access patterns.", "integrated"),
  c("GSI vs LSI", "GSI: alternative key, separate throughput, eventually consistent. LSI: same partition key, different sort key, created at table creation.", "conceptual"),
  c("Hot partitions", "A key that gets disproportionate traffic throttles. Fix with better key distribution, write sharding, or caching.", "conceptual"),
  c("Consistency", "Eventual is cheaper and faster; strong reads cost more. Choose by business need.", "conceptual"),
  c("Single-table design", "Multiple entity types in one table with prefixed keys, so related items are fetched with one query. Trade-off: harder to understand and evolve.", "integrated"),
  c("SQL vs NoSQL", "SQL for relations, ad-hoc queries and transactions. NoSQL for scale and key-based access.", "integrated"),
  c("Caching strategy", "Cache-aside with TTL, and invalidate on writes. Keys per tenant.", "conceptual"),
];
export const API: Concept[] = [
  c("HTTP status codes", "2xx success, 3xx redirect, 4xx client errors like 400, 401, 403, 404, 409, 422, 429, and 5xx server errors. Use them consistently.", "built"),
  c("PUT vs PATCH", "PUT replaces, PATCH partially updates. Both should be defined clearly.", "built"),
  c("Idempotency", "Repeating a request has the same effect. GET, PUT and DELETE are idempotent; POST needs an idempotency key.", "integrated"),
  c("Pagination", "Cursor pagination for large or changing data; return next cursor and limits.", "built"),
  c("Rate limiting", "Token bucket per client; return 429 with Retry-After. Clients should back off.", "built"),
  c("AuthN vs AuthZ", "Authentication verifies identity; authorisation decides access. Enforce both on the server.", "built"),
  c("JWT", "Signed token with claims. Validate signature, expiry, issuer and audience. Short-lived, with refresh.", "integrated"),
  c("OAuth 2 / OIDC basics", "OAuth 2 delegates authorisation; OIDC adds identity on top. Authorization code with PKCE for browser apps.", "conceptual"),
  c("CORS", "Browser rule for cross-origin requests; the server allows specific origins, methods and headers. It's not security for the API itself.", "built"),
  c("API versioning", "URL or header versioning; prefer additive changes and deprecate with notice.", "integrated"),
  c("OpenAPI / Swagger", "A machine-readable contract that generates docs, clients and mocks.", "conceptual"),
  c("GraphQL schema, queries, mutations, subscriptions", "Schema defines types; queries read; mutations write; subscriptions push.", "built"),
  c("N+1 and DataLoader", "Batch and cache per-request lookups to avoid one query per item.", "conceptual"),
  c("Error handling", "Consistent error shape with code, message and correlation ID. In GraphQL, check the errors array.", "built"),
  c("Retries, timeouts, circuit breakers", "Always set timeouts; retry idempotent calls with exponential backoff and jitter; open a circuit after repeated failures to protect the system.", "built"),
  c("API gateway", "Central entry for auth, limits and routing.", "integrated"),
  c("Third-party integrations", "Validate responses, handle pagination and rate limits, store secrets safely, and monitor failures. (My Uber work.)", "built"),
];
export const DEVOPS: Concept[] = [
  c("Git branching", "Short-lived feature branches off main, small pull requests, and squash or rebase merges for a clean history.", "built"),
  c("Merge vs rebase", "Merge preserves history with a merge commit; rebase rewrites commits for a linear history. Never rebase shared branches.", "built"),
  c("Pull requests & code review", "Small PRs, clear description, checks passing, and constructive review focused on correctness and readability.", "built"),
  c("CI/CD", "Automated lint, test, build and deploy on each change.", "integrated"),
  c("Dockerfile", "Small base image, multi-stage build, layer caching, non-root user.", "integrated"),
  c("Docker Compose", "Defines multi-container local environments in one file.", "integrated"),
  c("Kubernetes basics", "Pods run containers; Deployments manage replicas and rollouts; Services expose them; Ingress routes traffic; HPA scales.", "conceptual"),
  c("ECS / ECR", "ECS runs containers on AWS with simpler ops than Kubernetes; ECR stores images.", "conceptual"),
  c("Rolling, blue-green, canary", "Rolling replaces gradually; blue-green switches between environments; canary sends a small share first.", "conceptual"),
  c("Rollback", "Redeploy the previous artifact; make database changes backward-compatible.", "integrated"),
  c("Environment configuration & secrets", "Config through environment variables; secrets in a secrets manager, never in the repo.", "integrated"),
  c("Logging / monitoring / observability", "Logs for events, metrics for trends, traces for request paths. Alert on symptoms and SLOs.", "integrated"),
];

export const TS: Concept[] = [
  c("Why TypeScript", "Static types catch mistakes before runtime and document contracts between the frontend and the backend. The catch: types disappear at runtime, so I still validate real data at the boundaries.", "built"),
  c("interface vs type", "Both describe shapes. I use interfaces for object contracts like props and API models, since they extend cleanly, and type aliases for unions, intersections and mapped types.", "built", "Declaration merging?"),
  c("Discriminated unions", "A union where each variant has a literal 'kind' field, so the compiler narrows the type in a switch and forces me to handle every case. I'd use it for alert types or field kinds.", "built", "Exhaustiveness check with never?"),
  c("Generics", "They let me write reusable typed code, such as a table component or an API helper, without falling back to any. I keep them at the boundaries so types stay readable.", "built", "Constrain a generic with extends?"),
  c("Narrowing & type guards", "typeof, in, instanceof, equality and custom guards like isAlert(x): x is Alert let the compiler refine a union inside a branch.", "built"),
  c("unknown vs any", "any turns off checking; unknown forces me to narrow before using a value. For untrusted input, like API responses, I use unknown and validate.", "built"),
  c("Utility types", "Partial, Required, Pick, Omit, Record, ReturnType and Readonly derive new types from existing ones, so I don't duplicate shapes.", "built"),
  c("Runtime validation", "Because types don't exist at runtime, API responses, CSV rows and user input still need validation, for example with a schema library. Ideally one schema is shared by client and server.", "built", "Why not trust the compiler?"),
  c("Strict mode", "strict enables noImplicitAny, strictNullChecks and more. I keep it on, because null and undefined bugs are the most common source of runtime errors.", "built"),
  c("Typing API contracts", "Generate types from the GraphQL schema or OpenAPI spec so they can't drift from the backend, instead of hand-writing them.", "integrated"),
];
