import type { Kit } from "./kit-types";
const K = (n: number, s30: string, s120: string, simple: string, mine: string, danger: string, dk: string, key: string[], follows: Kit["follows"] = [], verify?: string): Kit => ({ n, s30, s120, simple, mine, danger, dk, key, follows, verify });
export const KITS_C: Kit[] = [
K(53, "The browser loads the React app from CloudFront and S3. The console signs in, then talks to AppSync over GraphQL. Resolver Lambdas read and write DynamoDB, slow work goes through SNS to workers, and results come back over a subscription. Telemetry lands in S3 through Firehose, and investigations query it with DuckDB.",
 "Let me trace it from the browser. The React app is served from S3 through CloudFront. After login, the console calls AppSync with a session token, and a Lambda authorizer checks it and sets the organisation. Queries and mutations run through resolver Lambdas, which use DynamoDB for agents, zones and policies. For heavy work, like an investigation query, the resolver publishes to SNS and returns a request ID; a worker reads Parquet files from S3 with DuckDB and pushes results back over a subscription. On the other side, agents send telemetry through Firehose into S3. What I personally handled was the browser side and the integration; the rest was owned by the backend and data teams.",
 "Browser, CDN, GraphQL, functions, database, queue, storage.", "A: browser side. C/E: the rest.", "Describing separate 'Endpoint Service' and 'Alert Service' with token refresh — that's the older bank answer (Q53) and doesn't match the AppSync architecture.", "I worked on the console side and integrated with the rest, so let me describe where my part started…", ["CloudFront", "AppSync", "Lambda", "DynamoDB", "SNS", "S3"], [], "Q53 differs from Q73/Q98 — use the Q73 version"),
K(159, "The hardest part was the visual query builder with nested AND/OR groups. The state is recursive, groups inside groups, and every change had to produce clean JSON that the backend accepts. I used a recursive component, kept the state in one place, and validated at the field, group and rule level.",
 "The nested query builder. Every group has its own AND/OR toggle, and it can contain conditions or other groups. So the state is a tree, and the UI is a recursive component. Three things made it hard. First, updating a node deep in the tree without mutating state. Second, keeping the UI simple for analysts even when the rule is complicated. Third, making sure the output JSON matched what the backend expected, exactly. I handled it with immutable updates, a single source of truth in context, memoised group components, and validation at three levels. Looking back, I made the nesting more general than the real use needed, so next time I'd cap the depth and ship the simpler version first.",
 "Recursive state, exact output format.", "A", "Claiming it was easy.", "N/A", ["recursive", "AND/OR", "JSON", "validation", "immutable"], [], "C-RB-BACKEND"),
K(209, "Freelancing taught me to take vague requirements and turn them into working software on my own: requirements, design, database, build, deployment and support. That's directly useful in any engineering team, because I don't need everything spelled out, and I communicate risks early.",
 "Between July 2023 and early 2024, I built and shipped sites for clients in India, the UAE, the UK and the US, including akemsgroup.com and nehadixit.com. Since I was the only developer, I did requirements, schema design, integrations like payments and forms, deployment and post-launch support. Three habits carried into my work at BluSapphire and Uber. First, asking clarifying questions and confirming assumptions before building. Second, breaking the work into milestones and telling people early when something is at risk. Third, thinking about maintainability, because I was the one supporting the site afterwards. It also taught me to explain technical trade-offs to non-technical people.",
 "Independence, requirements, communication.", "A", "Quoting revenue or project counts you can't defend; saying 'consulting'.", "N/A", ["requirements", "clients", "deployment", "communication", "milestones"], [], "C-FREELANCE, C-EY"),
K(220, "Multi-tenant data flow means the organisation travels with every request: from the login token, through the API layer, down to the data keys. Nothing trusts the client to say which organisation it is. That way one customer can never read another's data.",
 "The organisation has to be known at every hop, and it must come from a trusted source. In our platform, the login session carried the organisation, and the authorizer put it into the request context, so it couldn't be changed by the client. Resolvers used that value to build the data keys, so queries only touched that organisation's partition. In the UI, we only showed what that organisation could access. The trade-off with a shared platform is a bigger blast radius if there's a bug, so I'd add automated cross-tenant tests, and tenant-tagged logs so I can spot leaks.",
 "The tenant ID follows every request.", "C", "Saying the UI enforces it.", "Platform team owned the authorizer; here's how it fit together…", ["organisation", "token", "context", "partition", "test"], [], "C-AUTH"),
K(189, "Tenant isolation means each customer can only see their own data. We enforced it in three layers: identity, data and application. I handled the console's org scoping and the login integration; the identity setup and table design were the platform team's.",
 "Basically, three layers. The identity layer: the login token carries the organisation, and the authorizer sets it server-side in the request context, not from anything the client sends. The data layer: records are keyed by organisation, so a query for one organisation can't return another's items. The application layer: each operation is scoped to the caller's organisation, and the UI only offers what that organisation can see. To test it, I'd call every operation with another organisation's token and expect a denial. What I personally handled was the console's org handling and integrating the login flow.",
 "Three layers and a test.", "C/E", "'It's all in the UI.'", "N/A", ["identity", "data", "application", "server-side", "test"], [], "C-AUTH"),
K(153, "In Rule Builder, isolation works through an organisation code that's part of every call: schema lookups, query generation, lookup lists and rule saves are all scoped to it. Service-provider roles can switch between organisations, and client admins only see their own.",
 "Every API call includes the organisation code. The table schema query returns that organisation's columns, the query generator scopes the rule to it, the lookup lists are per organisation, and saved rules are tagged with it. The roles come from the login token: service-provider users can work across organisations, and client admins only see their own. I want to be clear that hiding things in the UI is only the convenience layer, and the server has to enforce the same rule. If I were redesigning it, I'd add cross-tenant tests for each endpoint.",
 "Org code on every call plus roles from the token.", "A/B", "'The UI hides other orgs, so it's safe.'", "N/A", ["organisation code", "roles", "server", "scoped"], [], "C-AUTH"),
K(40, "Basically, I agree the contract first with the backend engineers: the request and response shapes, error format and pagination. Then I build against that with typed models. I've read and reviewed Java services and debugged issues across the boundary, but the services were owned by the backend team.",
 "The frontend and the backend meet at a contract. In sprint planning we agree the shape of requests and responses, including errors and pagination. I mirror the contract in TypeScript types, so mismatches show up at compile time. For integration, I use loading, error and success states for every call, and I validate data at the boundary. When something breaks, I check the network tab, then the API response, then ask the backend engineer to look at the logs, and we often pair on it. I've reviewed Java and Spring code to understand the behaviour, but I'd say plainly that I integrate with those services rather than own them.",
 "Agree the contract, type it, handle failures.", "B/C", "Saying you wrote the Java services.", "I integrated with those services rather than owning them…", ["contract", "types", "errors", "pagination", "pair"], [], "C-SPRING"),
K(41, "Good REST design means resource-oriented URLs, correct verbs and status codes, a consistent error format, pagination, versioning, and authentication on every endpoint. We agreed those conventions between frontend and backend, and I integrated against them.",
 "The principles I care about: nouns for resources and verbs from HTTP; proper status codes such as 400, 401, 403, 404, 409 and 429; a consistent error body with a code, a message and a correlation ID; pagination and filtering for lists; idempotency for retries; versioning that avoids breaking clients; and authorisation on the server for every call. In Rule Builder I integrated with create, edit and clone endpoints, and agreed the payload shape with the backend. If I were improving it, I'd publish an OpenAPI spec and generate typed clients from it.",
 "Resources, verbs, status codes, errors, pagination, versioning.", "B", "Reciting verbs only.", "N/A", ["resource", "status codes", "error", "pagination", "versioning", "OpenAPI"], []),
K(44, "A DTO, or data transfer object, is a class that carries data across a boundary, like an API request or response. It keeps internal entities private and stabilises the contract. On my side I mirror those DTOs as TypeScript types.",
 "In Spring, DTOs sit between the controller and the domain model. You don't expose entities directly because that leaks internal structure, invites over-posting, and couples clients to your database. DTOs also let you shape responses, validate input with annotations, and change the internal model without breaking clients. From the frontend side, I define matching TypeScript interfaces and generate them from the schema where possible. I've used DTOs in personal Spring projects and consumed them at work.",
 "A safe, shaped copy of data for the API.", "B/D", "Confusing DTO with entity.", "I've used them in personal projects and consumed them at work…", ["DTO", "entity", "contract", "validation"], []),
K(49, "A typical flow: CloudFront and S3 serve the app, API Gateway or AppSync is the front door, Lambda holds the logic, DynamoDB or S3 stores data, SNS decouples slow work, IAM controls access, and CloudWatch shows what's happening.",
 "I think about it in layers. Delivery: CloudFront in front of a private S3 bucket. Entry: API Gateway for REST, or AppSync for GraphQL, with an authorizer. Compute: Lambda functions with small, single-purpose roles. Data: DynamoDB for key-based operational data, S3 for large files and Parquet analytics. Async: SNS, and SQS when I need buffering. Security: least-privilege IAM, encryption, and presigned URLs. Observability: CloudWatch logs, metrics and alarms. For each choice I can tell you the alternative and the trade-off, and my certification helped me see the full picture, not just the frontend part.",
 "Layers: delivery, entry, compute, data, async, security, observability.", "C", "Listing services with no reasons.", "N/A", ["CloudFront", "API Gateway", "Lambda", "DynamoDB", "SNS", "IAM", "CloudWatch"], [], "C-AUTH"),
K(52, "I'd map the data flow first, then choose the AWS pieces, define the API contract, and plan security, monitoring and rollout. For example, a scheduled report: a UI to configure it, an API to save it, a scheduler to trigger a function, and S3 plus email for the output.",
 "Let me use a scheduled-report feature. Step one: the user flow and the data flow, from the React form to the API to storage. Step two: choose services, for example API Gateway and Lambda to save the configuration to DynamoDB, EventBridge Scheduler to trigger a Lambda daily, S3 for the generated file, and SES for email. Step three: agree the API contract with the backend. Step four: security, like least-privilege roles and validating input. Step five: monitoring, meaning logs, an alarm on failures, and a dead-letter queue. Step six: infrastructure as code and a gradual rollout. I'd also write down the assumptions and trade-offs for review before building.",
 "Flow, services, contract, security, monitoring, code, rollout.", "D/C", "Jumping to services without the flow.", "N/A", ["data flow", "contract", "IaC", "monitoring", "security", "rollout"], []),
K(56, "I keep a clean contract between the frontend and each backend, handle loading, errors and retries at every integration point, validate data at the boundary, and use correlation IDs to trace a request across services.",
 "With multiple backends, the main risk is inconsistency. So first, typed contracts for each API. Second, a small API layer in the frontend that handles authentication, retries and error mapping, so components stay simple. Third, defined states for every call: loading, error and success, and no blank screens on failure. Fourth, validation of responses at the boundary, so a bad payload doesn't spread. Fifth, correlation IDs in requests and logs so I can trace a request across services. In the console, some data came through GraphQL and some through REST, and this structure kept them consistent.",
 "Contract, wrapper, states, validation, tracing.", "A/B", "N/A", "N/A", ["contract", "retry", "validation", "correlation ID"], [], "C-AUTH"),
K(29, "Smooth data flow comes from agreed contracts, validation at boundaries, sensible error handling, idempotency for retries, and monitoring. At Uber, I validate schemas, remove duplicates, and reconcile data between systems.",
 "Data has to move correctly between the frontend, the API, the services and the stores. I rely on five things. Clear contracts, agreed before building. Validation at every boundary, so bad data is rejected early. Error handling with retries and backoff, and idempotency so retries don't duplicate work. Reconciliation checks that compare sources, which is what I do for the hydration jobs at Uber. And monitoring, so drift or failures are noticed quickly. In the Rule Builder, the equivalent was step gating that rejected invalid rules before anything was saved.",
 "Contracts, validation, retries, reconciliation, monitoring.", "A", "Textbook definition only.", "N/A", ["contract", "validation", "idempotent", "reconcile", "monitor"], []),
K(94, "SNS is publish-subscribe. In our platform, mutations publish to SNS topics, for package builds and async queries, and return straight away; worker Lambdas do the heavy work. It decouples the fast response from slow processing.",
 "We used SNS in a few places. When an admin creates agent packages, the resolver publishes to a topic and returns immediately, and a separate Lambda builds the ZIP. For investigations, the startEdrAsyncQuery mutation publishes to a topic, and a worker runs the query. The benefit is that the user isn't blocked, and each side scales independently. The trade-offs: delivery is at-least-once, so workers must be idempotent; there's no replay like Kafka; and failures need a dead-letter queue. Our topics had retries and a DLQ, with an alarm on DLQ depth.",
 "Publish, return fast, worker does the work.", "C: the platform; A/C: client side.", "Saying you used Kafka.", "I worked with the SNS-based flow rather than owning the topics…", ["publish", "worker", "at-least-once", "dead-letter", "decouple"], []),
K(116, "Serverless means AWS runs the servers and scaling, and I pay per use. We chose it because the workload was bursty and event-driven, and a small team shouldn't spend time patching servers. The trade-offs are cold starts, limits, and observability across many functions.",
 "In our platform, Lambda is the compute, DynamoDB the database, Firehose the delivery, and AppSync the GraphQL layer, and none of them need servers to manage. It fitted because traffic was spiky: agents heartbeat steadily, but investigations come in bursts. The costs are cold starts on latency-sensitive paths, the fifteen-minute limit, concurrency limits, and harder tracing across many small functions. If I were redesigning it today, I'd add distributed tracing and dead-letter queues everywhere, and keep long or steady jobs on containers.",
 "No servers to run; pay per use.", "C", "'Serverless is always cheaper.'", "N/A", ["Lambda", "bursty", "cold start", "limits", "tracing"], []),
K(30, "Data protection means encrypting in transit and at rest, using least-privilege access, validating input, keeping secrets out of code, scoping data by tenant, logging for audit, and following the principle of defence in depth.",
 "The basics I apply: HTTPS everywhere and encryption at rest, for example SSE on S3 and KMS where needed. Least-privilege IAM roles. Strong authentication, with authorisation checked on the server, and tenant scoping for every query. Input validation and output encoding to prevent injection and XSS. Secrets in a secrets manager, never in the repository or the frontend. Audit logs for sensitive actions. And short-lived credentials, like presigned URLs with a short expiry. Security is layered, so a mistake in one place doesn't expose everything.",
 "Encrypt, least privilege, validate, keep secrets safe, scope by tenant.", "C", "Naming only 'HTTPS and JWT'.", "N/A", ["encryption", "least privilege", "validation", "secrets", "audit"], []),
K(89, "AppSync is AWS's managed GraphQL service. We chose it because the console needs aggregated queries and subscriptions, and AppSync provides both without us running a GraphQL server. REST remained for simple platform CRUD.",
 "AppSync gives me a schema, resolvers that connect to Lambda or DynamoDB, authorisation options, and subscriptions over WebSocket. For the console, it meant the frontend could ask for exactly the fields each screen needs, and get pushed results for asynchronous investigations. The trade-offs: resolver constraints, some debugging pain, lock-in, and cost for real-time connections. And GraphQL returns errors in the response body, so monitoring has to look at the errors field. An alternative would be Apollo Server on ECS, which gives more control at the price of more operations.",
 "Managed GraphQL with subscriptions.", "A/C", "'Better than REST.'", "N/A", ["schema", "resolvers", "subscriptions", "managed", "trade-off"], [], "C-AUTH"),
K(82, "SNS decouples the fast API response from slow background work. We used it for package builds, async investigation queries and package updates. The client gets a request ID straight away, and workers do the rest.",
 "There are three patterns. Package builds: createAgents publishes to SNS and returns, then a process Lambda builds the ZIP. Async queries: the mutation publishes and returns a request ID, and a worker runs DuckDB and pushes results over AppSync. Package updates: a new agent version triggers processing. Each one avoids blocking a request on slow work. The things I'd watch are idempotency, retries and DLQ handling.",
 "Return fast, work in the background.", "C", "N/A", "N/A", ["decouple", "async", "worker", "request ID"], []),
K(58, "Synchronous means the caller waits for the result; asynchronous means it gets an acknowledgement and the result arrives later. I use sync for quick reads and async for long-running work, like our investigation queries.",
 "With synchronous calls, the caller blocks until the answer arrives. It's simple, but fragile if the work is slow. With asynchronous calls, the caller gets an acknowledgement, such as a request ID, and the result comes later by callback, subscription or polling. In our console, fetching an agent list is synchronous, since it's fast. Running an investigation is asynchronous: mutation, SNS, worker, subscription. The cost of async is more moving parts, eventual results, and the need for failure handling.",
 "Wait now vs be told later.", "A/C", "N/A", "N/A", ["synchronous", "asynchronous", "request ID", "subscription"], []),
K(106, "Event-driven architecture means components communicate by publishing and reacting to events, instead of calling each other directly. We used it for package builds and async investigation, with SNS and subscriptions. It decouples systems and scales independently, but needs idempotency and good tracing.",
 "In an event-driven design, a producer publishes an event, and consumers react in their own time. That decouples them and lets them scale separately. In our platform, SNS carried events for package builds and asynchronous investigations, and AppSync subscriptions delivered the results to the console. The challenges: eventual consistency, duplicates and ordering, and harder debugging. So I'd use correlation IDs, idempotent consumers, dead-letter queues and versioned event schemas. I haven't used Kafka in production, but I understand where it adds replay and ordering.",
 "Publish events; others react.", "C", "Claiming Kafka.", "I haven't used Kafka in production, but…", ["publish", "consumer", "decouple", "idempotent", "eventual"], []),
K(60, "I debug integration issues layer by layer: the browser's network tab, then the API response, then server logs using a request ID, then the data. I change one thing at a time, and I agree the fix with the backend engineer.",
 "First I reproduce it and note the request. In the network tab, I check the URL, headers, payload and status, and whether the response matches the contract. If the response is wrong, I take the request ID to the backend logs and pair with the backend engineer. If it's right, the problem is on my side: mapping, state or rendering. I've seen contract mismatches, like a field coming back null instead of an empty array, so I add tests for those cases and raise them in code review.",
 "Network tab, response, logs, data — one layer at a time.", "A/B", "Guessing.", "N/A", ["network", "contract", "request ID", "logs", "one change"], []),
K(65, "We work in two-week sprints: planning, daily stand-ups, pull-request reviews, a demo and a retrospective. JIRA holds the stories and Confluence holds the design notes. I flag risks early.",
 "A typical sprint starts with planning, where we pick stories and agree the API contracts with backend engineers. Daily stand-ups take fifteen minutes. During the sprint, work goes through pull requests with at least one reviewer. At the end we demo and hold a retrospective to choose one improvement. I use JIRA for tracking, Confluence for documentation, and I try to raise blockers the day I see them.",
 "Plan, build, review, demo, improve.", "A", "Textbook Scrum.", "N/A", ["sprint", "stand-up", "pull request", "retrospective", "JIRA"], []),
K(18, "Python is a readable, high-level language with a great ecosystem. I've used it for Lambda handlers, scripts and automation. In enterprise teams it's common for automation, data processing and backend APIs.",
 "Python is popular for scripting and automation, data processing, and backend APIs with frameworks like FastAPI or Flask. On our platform, the agent-facing Lambda handlers were in Python, and I use scripting for automation. I'd say plainly that my strongest languages are TypeScript and JavaScript, then Python, with Java as the one I'm growing.",
 "Readable language for scripts, data and APIs.", "B/C", "Saying 'in enterprises like EY' — that's the wrong company.", "N/A", ["scripting", "automation", "Lambda", "FastAPI"], [], "C-EY"),
K(223, "NCC taught me discipline, teamwork and working under pressure. I completed the A, B and C certificates in the Navy wing. It's not technical, but it shaped how I work: I follow through, and I'm comfortable taking responsibility.",
 "It's not an engineering credential, but it's relevant to how I work. The training was about discipline, following structured processes, leading small teams and staying calm under pressure. I completed the A, B and C certificates. In engineering, I see it in how I follow through on commitments, communicate clearly during incidents, and take responsibility for what I ship.",
 "Discipline and teamwork.", "—", "Overselling it as technical.", "N/A", ["discipline", "teamwork", "pressure"], [], "C-EY"),
K(218, "I was a finalist in Smart India Hackathon 2022 in Nagpur. We built a prototype in a 36-hour sprint for a government problem statement. I learned to build an MVP quickly, split work in a team and present to non-technical judges.",
 "It was an intense 36 hours: define the problem, pick a scope we could finish, split the work across the team, and present to judges. The main lessons were prioritising ruthlessly, building a working slice rather than a complete product, and explaining the technical work in simple terms. I still use that habit when I build the core flow first and defer nice-to-haves.",
 "Fast MVP, teamwork, simple explanations.", "A", "N/A", "N/A", ["MVP", "team", "prioritise", "present"], [], "The 36-hour / team-of-6 details come from the bank, not the resume — confirm them"),
K(219, "I adapt to each team. With backend engineers, I talk API contracts and data; with security analysts, detection needs; with QA, test cases. I agree expectations early and keep everyone updated.",
 "At BluSapphire I worked with backend engineers, security researchers, QA and product. With backend, the conversation is about contracts and performance. With analysts, it's about what they need to detect and how they want to work. With QA, it's test cases and reproducible bugs. What helps most is agreeing the definition of done early, writing decisions down in Confluence, and raising blockers as soon as I see them. At Uber I work with operations teams, and config-driven tooling lets them change behaviour without a deployment.",
 "Speak each team's language; agree early.", "A/C", "N/A", "N/A", ["contracts", "analysts", "QA", "expectations"], []),
K(221, "A concrete example is zone management. I sat with a backend engineer to define the GraphQL mutation and its response, including a list of failures, wrote the matching TypeScript types, and in code review I noticed a field returning null instead of an empty array, which we fixed.",
 "When we built zone management, the backend engineer and I defined the mutation together: it takes an organisation, a zone and a list of agent IDs, and returns success plus a list of failed agents, because some moves can fail. I wrote the TypeScript interface to match. During review, I noticed the backend returned null for the failures list when everything succeeded, which would have crashed the UI. We agreed on an empty array. The lesson is to be explicit about edge cases in the contract, and to review each other's code across the boundary.",
 "A real contract discussion with a real edge case.", "A/C", "Making up a different example.", "N/A", ["mutation", "contract", "types", "review", "edge case"], [], "C-OWN"),
K(212, "Contract-first development, error handling at every integration point, and validation at the boundaries. I mirror the schema in TypeScript types, show retry states instead of blank screens, and check data before using it.",
 "Three practices. First, contract-first: we agree schemas before building, and I mirror them in types so mismatches show up early. Second, every API call has loading, error and success states, and failures show a retry option. Third, validation of responses at the boundary. At Uber I add de-duplication and reconciliation checks so data stays accurate between systems.",
 "Contract, error states, validation.", "A", "N/A", "N/A", ["contract", "error", "validation", "reconcile"], []),
K(213, "Performance monitoring is measuring latency, errors and resource use, then finding the bottleneck. I use CloudWatch for functions, browser DevTools and Lighthouse for the frontend, and Apollo DevTools for query timing.",
 "I start by measuring, not guessing. For the backend, CloudWatch shows duration, errors, throttles and memory. For the frontend, the network tab and Lighthouse show load and rendering costs, and Apollo DevTools shows query timing and cache behaviour. When a page is slow, I find whether the time is in the network, the server or the rendering, and fix the biggest cost first. Then I re-measure.",
 "Measure, find the biggest cost, fix, re-measure.", "A/C", "Quoting specific numbers you can't source.", "N/A", ["measure", "CloudWatch", "Lighthouse", "bottleneck"], []),
K(62, "I follow a structured process: assess impact, mitigate, gather evidence like logs and deploy history, reconstruct the timeline, test hypotheses, fix the root cause, and then prevent recurrence with tests, alerts and a short write-up.",
 "First, assess the impact and mitigate, for example with a rollback or a feature flag. Second, gather data: logs, metrics, deployment history, recent changes. Third, reconstruct the timeline: what changed before it started? Fourth, form and test hypotheses one at a time. Fifth, fix the root cause, not just the symptom. Sixth, prevent recurrence with a test, an alert and an update to the playbook, and share the write-up in a blameless way.",
 "Impact, evidence, timeline, hypothesis, fix, prevent.", "A", "Blaming a person.", "N/A", ["impact", "timeline", "hypothesis", "prevent", "blameless"], []),
K(26, "Splunk indexes machine data like logs so you can search, build dashboards and set alerts. I've used it for debugging from logs and dashboards, with structured logs and correlation IDs.",
 "You forward application logs to Splunk, it indexes them, and you query them with SPL to find errors, build dashboards and create alerts. I've used it to investigate production issues, and it works best when logs are structured with fields like request ID and organisation. I'd say my hands-on level is search and dashboards, rather than administering Splunk.",
 "Search and alert on logs.", "B", "Claiming Splunk admin.", "I've used it for searching and dashboards…", ["logs", "search", "dashboards", "alerts", "structured"], []),
K(87, "Regular HTTPS verifies the server to the client. Mutual TLS verifies both sides: the client presents a certificate too. Our agents used mTLS with API Gateway, with an authorizer checking the certificate.",
 "In normal HTTPS, only the server proves its identity. With mutual TLS, the client also presents a certificate, and the server verifies it. It's ideal for machine-to-machine traffic, like agents, since there are no passwords or API keys to leak. The trade-offs are certificate issuance, rotation and revocation. In our platform, agents presented a client certificate to API Gateway, and a custom authorizer validated it. That part was owned by the backend and platform team, and I understand how it fits with the console.",
 "Both sides show a certificate.", "E/D", "Claiming you built the certificate authority.", "The platform team owned it; here's how I understand it…", ["client certificate", "both sides", "authorizer", "rotation"], []),
K(90, "Lambda is on-demand compute. We used several, each with a single responsibility: agent APIs, GraphQL resolvers, authentication, async workers and package builders. Small functions are easier to secure, scale and debug.",
 "We had around six or more functions. One handles agent REST calls, one resolves the console's GraphQL operations, one validates the login token, and others do asynchronous work like query workers and package builds. Splitting by responsibility gives each function a narrow IAM role and independent scaling. The cost is more to observe and deploy, so consistent logging, correlation IDs and infrastructure as code matter. I contributed to and reviewed the resolver logic, and the backend team owned the rest.",
 "Small functions, one job each.", "C", "Claiming to have written all of them.", "N/A", ["single responsibility", "IAM", "scaling", "logging"], []),
K(95, "Stytch is a B2B authentication service. The console signs in with it and gets a session token; AppSync calls a Lambda authorizer that validates the token and returns the organisation. I integrated the login flow, and the platform team set up the identity provider.",
 "For the EDR console, the React app logs in through Stytch and gets a session JWT. That token goes to AppSync, where a Lambda authorizer validates it with Stytch and passes the organisation and roles into the resolver context. That's how every resolver knows which organisation it's serving. I integrated the login and session handling in the console; the provider configuration and authorizer were the platform team's. The Rule Builder is a different system, and used Keycloak SSO with a token exchange.",
 "Login gives a token; the authorizer trusts it; resolvers get the org.", "C", "Saying Cognito.", "The platform team owned the identity setup…", ["Stytch", "JWT", "authorizer", "organisation"], [], "C-AUTH"),
K(112, "IAM controls who can do what on which resource. We followed least privilege: each function had its own role with only the tables and buckets it needs. No wildcard permissions.",
 "Every Lambda has an execution role. The agent function can read and write the agent table and read the policy bucket, but can't touch the raw events bucket. That way a bug or compromise in one function has a small blast radius. I'd add regular reviews with IAM Access Analyzer, and avoid long-lived keys in favour of roles.",
 "Give each piece only the access it needs.", "C", "Using wildcards.", "N/A", ["least privilege", "role", "blast radius", "wildcard"], []),
K(113, "A dead-letter queue holds messages that failed processing after retries, so nothing is lost. Our SNS topics had one, and we alarmed on its depth.",
 "If a worker fails a message after a few retries, it goes to a dead-letter queue instead of disappearing. We monitored the DLQ depth in CloudWatch and alarmed when it was above zero. That lets us investigate and replay. It only works with idempotent workers, so a replay doesn't duplicate results.",
 "A safe place for failed messages.", "C", "N/A", "N/A", ["retries", "alarm", "replay", "idempotent"], []),
K(114, "CloudWatch collects logs, metrics and alarms. We used structured JSON logs with correlation IDs, custom metrics, and alarms tied to error rates, latency and queue depth.",
 "Lambda logs go to CloudWatch automatically. We added structured JSON logs with correlation IDs so a single request can be traced across functions, and custom metrics like enrolment rate and query duration. Alarms covered errors, throttles, p95 latency and DLQ depth. I'd add distributed tracing, and SLO-based alerts, to reduce noise.",
 "Logs, metrics, alarms.", "C", "N/A", "N/A", ["logs", "metrics", "alarms", "correlation ID"], []),
K(16, "Node.js runs JavaScript on the server, using the V8 engine and an event loop for non-blocking I/O. It's good for I/O-heavy APIs. In the browser there's no file system or process access. I use JavaScript automation at Uber.",
 "Node is a runtime for JavaScript outside the browser. It's single-threaded with an event loop, so it handles many concurrent I/O operations well, but CPU-heavy work blocks it, so I'd move that to workers or another service. Unlike the browser, it has file system and network access, and modules. At Uber, my automation scripts run on JavaScript, with retries, logging and scheduled jobs.",
 "JavaScript on the server with an event loop.", "A", "Claiming a large Node backend.", "N/A", ["event loop", "non-blocking", "V8", "I/O"], []),
K(25, "TDD means writing a failing test first, then the code, then refactoring. BDD describes behaviour in plain language, like Given-When-Then, so business and engineering share it. In practice, I write tests for logic that could break users, and I'd use BDD-style names for clarity.",
 "In TDD you write a small failing test, make it pass, then refactor. It gives fast feedback and a safety net. BDD focuses on behaviour, written as Given, When, Then, which helps non-engineers read the tests. I don't follow TDD dogmatically; I use tests first for tricky logic, like generators and validators, and after for UI. What matters is that critical behaviour is covered and tests run in CI.",
 "Test first vs behaviour in plain language.", "A/D", "Claiming strict TDD.", "N/A", ["failing test", "refactor", "Given-When-Then", "CI"], []),
K(13, "Git tracks changes. I use short-lived feature branches, small pull requests and code reviews. I merge or rebase carefully and never rewrite shared history.",
 "I branch from main, commit in small logical steps, and open a pull request with a clear description. Reviews are required, and CI must pass. For updating a branch I prefer rebase for a linear history, but I never rebase shared branches. I use squash merges to keep main readable. For mistakes, I use revert rather than rewriting history.",
 "Small branches, small PRs, reviews.", "A", "N/A", "N/A", ["branch", "pull request", "review", "rebase", "merge"], []),
K(20, "RDS is AWS's managed relational database. It handles backups, patching and Multi-AZ failover. It's a good fit when you need SQL, joins and transactions. I'd choose it over DynamoDB for relational data and reporting.",
 "RDS runs engines like PostgreSQL or MySQL for you. The benefits are automated backups, failover and read replicas. The trade-offs are vertical scaling limits and connection management, especially with Lambda, where I'd use a proxy. On our platform we used DynamoDB for operational data, but I understand when RDS is the better choice, like relational reporting or complex queries.",
 "Managed SQL.", "D", "Claiming RDS production ownership.", "I haven't run RDS in production, but…", ["managed", "Multi-AZ", "SQL", "connections"], []),
K(19, "ECS runs containers on AWS without the complexity of Kubernetes. I haven't used it in production; on our platform we used Lambda. I'd choose ECS for steady, long-running services.",
 "ECS schedules containers, with Fargate removing server management. You define tasks and services, with autoscaling and load balancers. It's simpler than Kubernetes but AWS-specific. I know where it fits: steady or long-running workloads, and services that outgrow Lambda's limits. My own experience has been with Lambda and Docker for builds.",
 "Run containers on AWS.", "D", "Claiming ECS production experience.", "I haven't run ECS in production, but…", ["containers", "Fargate", "service", "autoscaling"], []),
K(24, "Kinesis is AWS's streaming platform. Data Streams gives ordered, replayable streams with shards; Firehose delivers streaming data to S3 without consumers to manage. Our agents used Firehose to deliver to S3.",
 "Kinesis Data Streams is for custom consumers, ordering per shard and replay. Firehose is managed delivery: it buffers, compresses and writes to S3. On our platform, agents sent telemetry to Firehose, which wrote gzipped files partitioned by date and organisation, and a pipeline converted them to Parquet. Firehose is at-least-once, so downstream must handle duplicates. The pipeline was owned by the data team.",
 "Streams for replay; Firehose for delivery.", "E/D", "Saying 'thousands of events per second through Streams' — not supported by code-derived answers.", "The data team owned that pipeline…", ["Firehose", "S3", "Parquet", "at-least-once"], [], "C-KINESIS"),
];
