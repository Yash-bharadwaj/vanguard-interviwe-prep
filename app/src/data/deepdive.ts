export interface Level { level: string; simple: string; tech: string; follow: string; best: string; flag?: string }
export const EDR_TREE: Level[] = [
  { level: "What is EDR?", simple: "A security camera system for computers: an agent watches each laptop/server and raises alerts on suspicious activity.", tech: "Endpoint Detection & Response: agents collect process/file/network/registry telemetry; a backend correlates it with rules; analysts investigate in a console.", follow: "How is it different from antivirus?", best: "Antivirus mostly blocks known-bad files; EDR records behaviour so analysts can detect, investigate and respond after the fact." },
  { level: "Business problem", simple: "Analysts needed one place to monitor 500+ endpoints, investigate incidents and manage policies without depending on support.", tech: "Console for endpoint monitoring, policy/zone management, threat analytics; self-serve installers; investigation queries.", follow: "Who were the users?", best: "Security analysts and org admins across multiple customer organisations." },
  { level: "Architecture", simple: "Two halves: one talks to agents on machines, one serves the analyst console.", tech: "Agent stack: API Gateway (mTLS) → Python Lambda for enrolment/heartbeat/config/policy. Console stack: AppSync GraphQL + Lambda resolvers + DynamoDB; subscriptions for async results. Data: Firehose → S3 (gzip JSON) → Parquet lake.", follow: "Why two stacks?", best: "Different trust models and traffic patterns: agents are machine identities with mTLS; analysts are human users with SSO/JWT." },
  { level: "Endpoint agent", simple: "A small program installed on each computer that reports to us.", tech: "Installer package per org; enrols with a certificate, sends heartbeats, fetches policy, ships telemetry.", follow: "How does the agent authenticate?", best: "Mutual TLS: the agent presents a client cert validated by an API Gateway custom authorizer. I integrated with this; the backend owned it.", flag: "Backend-owned — say 'I understand / integrated', not 'I built'." },
  { level: "Event ingestion", simple: "Agents send raw events to AWS, which stores them cheaply in batches.", tech: "Agent → Kinesis Data Firehose (temporary IAM creds) → gzipped JSON in S3 partitioned by date/org → pipeline converts to Parquet.", follow: "Why Firehose, not Kinesis Streams?", best: "We only needed managed delivery to S3, not custom consumers or replay; Streams adds shard management.", flag: "Data platform was backend-owned." },
  { level: "Kinesis / event flow", simple: "Managed pipe that buffers and delivers streaming data.", tech: "Firehose buffers by size/time, compresses, delivers to S3.", follow: "What about ordering/duplicates?", best: "Firehose is at-least-once, so downstream must be idempotent/dedupe; ordering isn't guaranteed across batches." },
  { level: "Backend", simple: "Serverless functions handle requests; no servers to manage.", tech: "Python Lambdas behind API Gateway (agents) and AppSync resolvers (console).", follow: "Did you write the Lambdas?", best: "I contributed to resolver logic and reviewed the code, and owned the console side of the contract; backend engineers owned the handlers.", flag: "See C-OWN." },
  { level: "GraphQL", simple: "The console asks for exactly the fields it needs in one request.", tech: "AppSync GraphQL schema: queries (agent lists, alerts), mutations (startEdrAsyncQuery, downloadAgent), subscriptions (onEdrAsyncQueryResponse).", follow: "Why GraphQL over REST?", best: "Many different views over related data, plus first-class subscriptions from a managed service. Trade-off: caching/rate-limiting are harder than plain REST." },
  { level: "WebSocket / subscriptions", simple: "The server pushes results to the browser when ready.", tech: "AppSync subscriptions over WebSocket, filtered by requestId.", follow: "What if the WebSocket drops?", best: "The client reconnects and can re-query by requestId; for lists we also poll every ~30s.", flag: "Don't claim a custom WebSocket manager unless you built one (C-RT)." },
  { level: "React console", simple: "The screens analysts use.", tech: "React + TypeScript + Apollo Client; code-splitting; virtualized tables.", follow: "How is it structured?", best: "Feature folders, reusable table/filter/alert components, data hooks per view, route-level lazy loading." },
  { level: "Data flow", simple: "Click Investigate → request goes out → results stream back.", tech: "mutation → Lambda → SNS → worker → DuckDB over S3 Parquet → AppSync mutation → subscription → UI.", follow: "Where can it fail?", best: "Publish failure, worker crash, query timeout, subscription drop — each has a retry/DLQ/re-query path." },
  { level: "DynamoDB", simple: "Fast key-value database for agents, zones, policies.", tech: "Single-table with entity-prefixed keys (AGENT##org, sort key endpointID); every access pattern is GetItem/Query.", follow: "Why DynamoDB, not PostgreSQL?", best: "Known access patterns, single-digit ms at scale, org-partitioned data, no servers. Cost: no ad-hoc joins; redesign needed for new access patterns.", flag: "Table design was backend-owned — 'I understand it and its access patterns'." },
  { level: "S3", simple: "Cheap durable file storage.", tech: "Several buckets: raw events, data lake, agent packages, static site.", follow: "Security?", best: "Block public access, SSE encryption, presigned URLs for controlled downloads." },
  { level: "Lambda", simple: "Code that runs only when called.", tech: "Small single-purpose functions: resolvers, workers, package builders.", follow: "Cold starts / limits?", best: "Cold starts matter for latency-sensitive resolvers; long jobs go async (SNS worker) because of the 15-minute limit." },
  { level: "AppSync", simple: "Managed GraphQL service.", tech: "Resolvers to Lambda/DynamoDB, auth via Lambda authorizer, subscriptions.", follow: "AppSync vs Apollo Server on ECS?", best: "AppSync removes server ops and gives subscriptions; Apollo Server gives more control/portability at higher ops cost." },
  { level: "SNS", simple: "A broadcast/decoupling channel.", tech: "Publish from API Lambda, subscribers do slow work (package build, async query).", follow: "SNS vs SQS vs Kafka?", best: "SNS fan-out with retries; SQS for buffered single-consumer work; Kafka for ordered replayable streams — I've used SNS, not Kafka." },
  { level: "Authentication", simple: "Users log in; machines use certificates.", tech: "Console: Stytch B2B session JWT validated by a Lambda authorizer producing org context. Agents: mTLS.", follow: "AuthN vs AuthZ here?", best: "AuthN = who (JWT); AuthZ = org scoping + roles applied in resolvers.", flag: "Never mention Cognito (C-AUTH)." },
  { level: "Tenant isolation", simple: "One customer must never see another's data.", tech: "Three layers: org in the token → org-partitioned data keys → app-level checks.", follow: "How would you test it?", best: "Automated tests that call every resolver with a different org's token and expect empty/denied." },
  { level: "Monitoring", simple: "Dashboards and alerts tell us when something breaks.", tech: "CloudWatch metrics/logs, SLO dashboards, playbooks.", follow: "What would you alert on?", best: "Error rate, p95 latency, throttles, DLQ depth, subscription failures." },
  { level: "Performance", simple: "Make big screens feel fast.", tech: "Code splitting, virtualized lists, CDN; sub-2s loads on large datasets.", follow: "How did you measure?", best: "Browser profiling and load-time checks before/after each change.", flag: "Only the sub-2s figure is on the resume." },
  { level: "Scaling", simple: "More customers/endpoints shouldn't break it.", tech: "Serverless components scale per request; data partitioned by org; async for heavy work.", follow: "10× traffic?", best: "Watch Lambda concurrency/throttles, DynamoDB hot partitions, AppSync limits; add caching and pagination." },
  { level: "Failure handling", simple: "Plan for things to break.", tech: "Retries, DLQ, idempotent workers, timeouts, re-subscribe.", follow: "Worker fails mid-query?", best: "SNS retries then DLQ; worker is idempotent by requestId; client shows failure and allows re-run." },
  { level: "Testing", simple: "Check behaviour automatically.", tech: "Unit tests for components/hooks, integration tests around API contracts, manual/staging verification.", follow: "What would you add?", best: "Contract tests for GraphQL operations and authorization tests.", flag: "Name only tools you truly used (Vitest/Playwright are unverified)." },
  { level: "Deployment", simple: "Ship without downtime.", tech: "Build → S3 → CloudFront invalidation; infra via CDK.", follow: "Rollback?", best: "Keep previous build artifact, re-point/redeploy and invalidate; versioned assets make it safe." },
  { level: "Security", simple: "Protect users' data and access.", tech: "Least-privilege IAM, mTLS for agents, JWT validation, encryption at rest, presigned URLs, org scoping.", follow: "Biggest risk?", best: "Broken tenant isolation — hence enforcement at multiple layers and tests." },
];
export const RB_TREE: Level[] = [
  { level: "Requirements", simple: "Let analysts create detection rules without engineers.", tech: "Create/edit/clone rules; nested conditions; reusable lists; validated output aligned with the engine's contract.", follow: "Who decided the steps?", best: "Product + analysts + backend contract; I proposed the step order to gate errors early." },
  { level: "UI architecture", simple: "A guided wizard.", tech: "React 18 + Vite + TypeScript SPA, MUI, react-router; wizard state in one Context.", follow: "Why Context not Redux?", best: "State is scoped to one workflow; adding Redux would be ceremony." },
  { level: "Schema-driven design", simple: "The form shape comes from data.", tech: "getColumns GraphQL query returns table schemas; fields/operators render from it.", follow: "Why?", best: "New fields appear without frontend releases; cost is upfront abstraction." },
  { level: "Recursive components", simple: "Groups can contain groups.", tech: "RuleGroup → (RuleCondition | RuleGroup) with AND/OR combinators.", follow: "Depth limits?", best: "Practical depth is small; I'd cap nesting and simplify if unused (over-engineering lesson)." },
  { level: "Validation", simple: "Stop bad rules before saving.", tech: "Step gating + field-, group- and rule-level validation.", follow: "Duplicated with the server?", best: "Yes — client validation is UX; server remains authority." },
  { level: "JSON/SQL generation", simple: "Turn clicks into the exact format the engine needs.", tech: "Builder emits JSON and SQL; Review step shows both; Flink query generated by a FastAPI GraphQL service.", follow: "How do you keep payloads compatible?", best: "Typed contracts + review with backend + example fixtures." },
  { level: "API", simple: "Call the right endpoint for the action.", tech: "REST create/edit/clone endpoints on the platform; Axios; token exchange for API auth.", follow: "PUT vs PATCH?", best: "Edit = replace full rule (PUT-like) since the wizard holds the full object." },
  { level: "Lambda / DynamoDB", simple: "Backend storage.", tech: "Conflict: bank Q48 says Lambda+DynamoDB; code-derived answers say platform REST + FastAPI.", follow: "Where are rules stored?", best: "Say what you can verify: 'the platform's rule service persists them; I integrated via REST.'", flag: "C-RB-BACKEND — verify first." },
  { level: "CRUD", simple: "Create, read, update, delete rules and lists.", tech: "List/detail views; mutations through REST; optimistic UI where safe.", follow: "Concurrent edits?", best: "Last-write-wins today; I'd add version/ETag to detect conflicts." },
  { level: "Clone", simple: "Start from an existing rule.", tech: "Load rule → reset identity fields → open wizard in clone mode → POST as new.", follow: "What resets?", best: "ID, name suffix, audit fields." },
  { level: "Lookup lists", simple: "Reusable whitelists.", tech: "Per-org lists with MUI DataGrid CRUD; referenced by isInPredefinedList.", follow: "Why separate lists?", best: "Reuse across rules and update once." },
  { level: "CSV upload", simple: "Bulk-load list entries.", tech: "Parse in browser, validate/dedupe, preview, then submit to the list service.", follow: "Large files?", best: "Chunk/stream or upload to S3 via presigned URL and process server-side." },
  { level: "Authentication", simple: "Single sign-on.", tech: "Keycloak check-sso → JWT → token exchange → platform api_token.", follow: "Why exchange?", best: "The REST platform predates Keycloak; exchange bridges without rewriting it." },
  { level: "Authorization", simple: "Roles decide what you can do.", tech: "Roles from JWT (serviceprovideradmin/analyst/clientadmin) toggle UI; server enforces.", follow: "Is UI hiding enough?", best: "No — UI hiding is convenience; the API must enforce." },
  { level: "Tenant isolation", simple: "Only your org's data.", tech: "org code param on every call; SP roles can switch orgs.", follow: "Risk?", best: "Missing org scope on one endpoint — test each." },
  { level: "Performance", simple: "Keep wizard snappy.", tech: "Memoised nodes, lazy step content, debounce.", follow: "Deep trees?", best: "Memoise group subtrees; avoid context updates per keystroke where possible." },
  { level: "Testing", simple: "Verify logic and flows.", tech: "Unit tests for validators/generators; flow tests for wizard.", follow: "Golden files?", best: "Snapshot JSON/SQL for known rules." },
  { level: "Deployment", simple: "Static SPA on CDN.", tech: "Vite build → S3/CloudFront; env-specific configs.", follow: "Env variables?", best: "Build-time VITE_ vars; never secrets." },
  { level: "Trade-offs", simple: "Flexibility vs simplicity.", tech: "Schema-driven + recursive generality vs YAGNI; duplicate validation; Context re-renders.", follow: "What would you change?", best: "Add shared validation schema, versioned contracts, and simpler nesting." },
];
