import type { Kit } from "./kit-types";
const K = (n: number, s30: string, s120: string, simple: string, mine: string, danger: string, dk: string, key: string[], follows: Kit["follows"] = [], verify?: string): Kit => ({ n, s30, s120, simple, mine, danger, dk, key, follows, verify });
export const KITS_B: Kit[] = [
K(6, "React is a JavaScript library for building interfaces out of reusable components. I use it because it's declarative: I describe what the UI should look like for a given state, and React works out the updates. It also has a strong ecosystem, like Apollo, MUI and the router, which helps a small team move fast.",
 "Basically, React lets me build the UI from small components, and I describe the result for a given state instead of manually changing the DOM. When state changes, React re-renders and compares the new output with the previous one, and only updates what actually changed. In our consoles that mattered, because screens had tables, filters and live data. I'd say the honest trade-off is that React is only a library, so the team has to choose routing, state and forms, which can lead to inconsistency without conventions. One more thing: React isn't fast just because of the virtual DOM. It's fast enough when you avoid unnecessary re-renders, keep state close to where it's used, and virtualize big lists.",
 "Build the UI from reusable pieces that update when data changes.", "A: frontend.", "Saying 'React is faster because of the virtual DOM'.", "N/A", ["components", "declarative", "state", "re-render", "trade-off"],
 [{ q: "What's the virtual DOM?", a: "It's an in-memory representation of the UI. React compares the previous and new trees and applies only the differences to the real DOM. It isn't magic; it's a way of keeping updates predictable, and it's not automatically faster than careful hand-written DOM code." }]),
K(7, "TypeScript adds static types on top of JavaScript, so the compiler catches mistakes before runtime. In a team, types act as documentation for API shapes and props. The catch is that types disappear at runtime, so I still validate data at the boundaries.",
 "I use TypeScript because multiple people work on the same code and it talks to backend contracts. If a backend field changes, the compiler shows me every place that breaks. I model variants with discriminated unions, like alert types, so the compiler makes me handle every case, and I use generics for reusable tables and API helpers. The important limitation is that types don't exist at runtime. So API responses, CSV uploads and user input still need runtime validation, for example with a schema library. If I were redesigning, I'd generate types from the GraphQL schema or OpenAPI spec so they can't drift.",
 "Types catch mistakes early; validate real data at runtime.", "A", "Claiming TypeScript guarantees data correctness.", "N/A", ["types", "compile", "runtime", "validation", "unions"], [{ q: "Interface vs type?", a: "Both describe shapes. I use interfaces for object contracts like props and API models, and type aliases for unions, intersections and mapped types. Interfaces can be extended and merged; unions need type." }]),
K(8, "REST is an architectural style where resources have URLs and you use HTTP methods, like GET, POST, PUT and DELETE, to act on them. It's stateless, cacheable, and easy for every team to understand. Good REST APIs use the right status codes, consistent errors, pagination and idempotent operations where possible.",
 "In REST, everything is a resource, like a rule or an agent, and the HTTP verb is the action. GET reads, POST creates, PUT replaces, PATCH partially updates, and DELETE removes. It's stateless, so every request carries what the server needs, which makes it easy to scale and cache. For a solid API, I care about a few things: correct status codes; a consistent error format; pagination for lists; idempotency for retries, such as idempotency keys on POST; versioning to avoid breaking clients; and authentication and authorisation on every endpoint. In Rule Builder, I consumed REST endpoints for rule create, edit and clone, and I agreed the payload contract with the backend.",
 "Resources plus HTTP verbs, stateless.", "B: consumed and agreed contracts.", "Reciting verbs with no reliability thinking.", "N/A", ["resource", "HTTP", "stateless", "idempotent", "status codes", "pagination"], [{ q: "PUT vs PATCH?", a: "PUT replaces the whole resource, and repeated calls give the same result. PATCH changes only some fields. In the wizard, the full rule object exists in the client, so replacing it with PUT was natural." }, { q: "How do you make POST safe to retry?", a: "Send an idempotency key. The server stores the key with the result, and if the same key arrives again it returns the original response instead of creating a duplicate." }]),
K(9, "Java is a statically-typed, object-oriented language that runs on the JVM. Enterprises use it for long-lived backend services because of its maturity, tooling, performance, and the Spring ecosystem. I know the core concepts well, and I'm honest that my production work has leaned towards React and AWS.",
 "Java compiles to bytecode that runs on the JVM, which handles memory and garbage collection. It's strongly typed and object-oriented, with a huge ecosystem, and Spring Boot is the usual way to build services. The concepts I'd expect to be asked about are: OOP and SOLID; collections, like how HashMap works with hashCode and equals; exceptions, checked versus unchecked; streams and Optional; concurrency basics; and the JVM memory model and garbage collection. In my case, I've used Java through personal Spring Boot projects and by working alongside Java services, so I'm confident on the fundamentals, but I wouldn't claim years of production Java ownership.",
 "Typed, object-oriented, JVM-based, widely used in enterprises.", "D/B", "Pretending to be a senior Java engineer.", "I haven't owned that in production, but the concept is…", ["JVM", "OOP", "collections", "exceptions", "Spring"], [{ q: "How does HashMap work?", a: "It hashes the key with hashCode to pick a bucket. Within the bucket, it uses equals to find the exact key. So if you override equals, you must override hashCode. Collisions are handled with a linked list or a tree in a bucket, and it resizes when the load factor is exceeded." }, { q: "Checked vs unchecked exceptions?", a: "Checked exceptions must be handled or declared at compile time, like IOException. Unchecked ones, like NullPointerException, extend RuntimeException and aren't forced. I use checked ones for recoverable conditions and unchecked for programming errors." }, { q: "== vs equals?", a: "== compares references for objects, while equals compares logical equality if it's overridden. Strings should be compared with equals." }]),
K(10, "Spring Boot is a framework that makes it fast to build production-ready Java services. It gives you auto-configuration, embedded servers, dependency injection, and starters for web, data and security. The usual structure is controller, service, repository.",
 "Spring Boot removes most of the setup. You add starters, and auto-configuration wires beans based on what's on the classpath. A typical service has a controller layer for HTTP, a service layer for business logic with @Transactional, and a repository layer using Spring Data JPA. Dependency injection means Spring builds and supplies the objects, which makes testing easy. Then there's validation with annotations, centralised exception handling with @ControllerAdvice, Spring Security for authentication and authorisation, profiles for configuration per environment, and Actuator for health and metrics. My honest level: I've built these pieces in personal projects and I've reviewed and integrated with Spring services at work, but I haven't owned a big Spring service in production.",
 "Framework that wires everything so you focus on business logic.", "D/B", "Saying 'I work with Spring Boot daily'.", "I haven't owned it in production, but I understand it…", ["controller", "service", "repository", "dependency injection", "JPA", "security", "Actuator"], [{ q: "How would you secure a Spring Boot API with JWT?", a: "A security filter reads the token, validates the signature and expiry, and sets the authentication in the security context. Then I use role-based rules on endpoints or methods. I've consumed JWT-based login as a client, and built this pattern in a personal project." }, { q: "What is a bean?", a: "An object managed by the Spring container. It's created, wired and destroyed by Spring, usually via annotations like @Component, @Service or @Bean methods." }], "C-SPRING"),
K(11, "AWS is Amazon's cloud platform. On my projects I've used Lambda for compute, API Gateway and AppSync for APIs, DynamoDB for data, S3 and CloudFront for storage and delivery, SNS and Firehose for events, and IAM for access. I'm AWS Developer Associate certified.",
 "The services I've worked with, grouped by job: compute is Lambda; APIs are API Gateway for REST and agent traffic, and AppSync for managed GraphQL; data is DynamoDB for operational records and S3 with Parquet for analytics; delivery is CloudFront in front of S3 for the static app; events are SNS for decoupling and Firehose for delivering streaming data to S3; security is IAM, with least-privilege roles, and presigned URLs; and infrastructure is CDK. For each of those I can tell you why we picked it, the alternative, and the trade-off. For example, Lambda gives per-request scaling but has cold starts and time limits, so long work goes to a worker through SNS.",
 "Group services by job, not by list.", "C: worked with backend/DevOps.", "Listing 15 services without why.", "N/A", ["Lambda", "AppSync", "DynamoDB", "S3", "CloudFront", "SNS", "IAM"], []),
K(12, "REST exposes many endpoints, each returning a fixed shape. GraphQL exposes one endpoint where the client asks for exactly the fields it needs, and it supports subscriptions. I used GraphQL for the console's aggregated views and REST for simple platform CRUD. GraphQL isn't always better; caching and query cost are harder.",
 "In REST, the server decides the shape of each resource, so a screen that needs related data may need several calls, or receive more than it needs. In GraphQL, the client sends a query describing exactly the fields, in one round trip, and the same schema supports mutations and subscriptions. In our console we used AppSync for views that combine agents, policies and alerts, and for the async subscription. We used REST for straightforward resource operations on the platform. The trade-offs of GraphQL: HTTP caching is harder, resolvers can cause the N+1 problem, and you need depth and cost limits. Also, errors often return HTTP 200 with an errors array, which changes how you monitor. So I pick per use case rather than by preference.",
 "GraphQL: ask for exactly what you need. REST: simple resources.", "A/B", "'GraphQL is always better.'", "N/A", ["one endpoint", "fields", "subscriptions", "N+1", "caching"],
 [{ q: "What's the N+1 problem?", a: "A resolver for a list calls the database once per item, so one list of N items causes N extra queries. The fix is batching, for example with DataLoader, so related lookups are grouped into one call." }, { q: "How do subscriptions work?", a: "The client opens a WebSocket and registers a subscription. When the server publishes a matching event, it pushes the data to the client. In AppSync, a mutation can trigger subscriptions, and clients filter by arguments such as a request ID." }]),
K(14, "Docker packages an application with its dependencies into a container, so it runs the same everywhere. I use it for consistent development and builds. For images, I'd use multi-stage builds, a small base, and run as non-root.",
 "A container is a lightweight, isolated process with its own filesystem, built from an image. The benefit is repeatability: what runs on my laptop runs in the pipeline and in production. Compared with a virtual machine, containers share the host kernel, so they start faster and use less memory, though isolation is weaker. A good Dockerfile uses a small base image, multi-stage builds to keep build tools out of the final image, pinned versions, a .dockerignore, and a non-root user. In my work, Docker has been for dev and build consistency; I haven't operated Kubernetes clusters, though I understand what they add: scheduling, scaling, and self-healing.",
 "Package the app plus everything it needs.", "C", "Claiming Kubernetes operations.", "I haven't run Kubernetes in production, but…", ["container", "image", "isolation", "multi-stage", "non-root"], []),
K(15, "In practice, we work in two-week sprints with planning, daily stand-ups, review and a retrospective. Stories live in JIRA, documentation in Confluence, and every change goes through code review. I raise blockers early rather than waiting.",
 "Our rhythm is a sprint of about two weeks. In planning we pick stories, estimate them with the team, and agree on API contracts with the backend engineers who'll be involved. We have a short daily stand-up: what I did, what I'm doing, what's blocking me. Work goes through pull requests with at least one review. At the end there's a review or demo, and a retrospective where we pick one improvement to try. I use JIRA for tracking and Confluence for design notes and decisions. My habit is to communicate risk early, for instance, flagging a dependency on another team in the stand-up rather than at the end of the sprint.",
 "Real ceremonies, real examples.", "A", "Textbook definitions.", "N/A", ["sprint", "stand-up", "retrospective", "JIRA", "review"], []),
K(22, "SNS is publish-subscribe: one message fans out to many subscribers. SQS is a queue: consumers pull messages and process them at their own pace. Together they give fan-out with buffering. I've used SNS, and I'd add SQS with a dead-letter queue if I needed back-pressure.",
 "SNS pushes a message to every subscriber, which is good for decoupling and fan-out. SQS stores messages until a consumer processes them, which gives buffering, retries, and a dead-letter queue for failures. A common pattern is SNS to several SQS queues, so each consumer works independently. Kafka is different again: a durable, ordered, replayable log with consumer groups, which is useful when you need replay or strict ordering per key. On our platform I've used SNS to decouple a fast API response from slow work, like the async investigation. With SNS or SQS standard queues delivery is at-least-once, so consumers must be idempotent. I haven't run Kafka in production, but I understand where it fits.",
 "SNS broadcasts, SQS queues, Kafka keeps a log.", "C: SNS via the platform. D: Kafka conceptual.", "Saying you used Kafka.", "I haven't used Kafka in production, but I've worked with event-driven patterns on AWS, so the concepts I know are…", ["fan-out", "queue", "dead-letter", "at-least-once", "idempotent", "Kafka"],
 [{ q: "How would you handle duplicates?", a: "Make the consumer idempotent: use an idempotency key or a natural key, store processed IDs, and design writes so repeating them has no extra effect." }, { q: "When would you pick Kafka?", a: "When I need a durable, replayable event log, strict ordering per key, many independent consumers reading at their own pace, or stream processing. For simple decoupling, SNS/SQS is less to operate." }]),
K(23, "Infrastructure as Code means defining cloud resources in code, so environments are repeatable and reviewable. CloudFormation is AWS's native tool; CDK lets you write it in a programming language and generates CloudFormation; Terraform is multi-cloud with its own state. I've worked with CDK.",
 "The benefit is that infrastructure lives in version control, gets code review, and can be recreated the same way in every environment. CloudFormation uses templates, and it's the AWS-native option. CDK lets you write those templates in TypeScript or Python with loops and types, and it synthesises CloudFormation. Terraform works across clouds and keeps state in a backend, which is powerful but needs care, like locking and drift detection. My hands-on experience is with CDK; I understand Terraform and plain CloudFormation conceptually and I'd be comfortable ramping up on whichever the team uses.",
 "Infrastructure as reviewed, versioned code.", "C/D", "Claiming Terraform experience.", "I've used CDK and understand Terraform conceptually…", ["CloudFormation", "CDK", "Terraform", "state", "drift"], []),
K(27, "For SQL, I focus on the basics that matter in practice: joins, indexes, transactions, normalisation, and reading query plans. If a query is slow, I measure first, look at the plan, check indexes, and watch for N+1 patterns.",
 "The concepts I rely on are: joins, to combine tables; indexes, to speed up lookups at the cost of slower writes; transactions and ACID for correctness; isolation levels and locking for concurrent access; normalisation to avoid duplication, and denormalisation where read speed matters. To optimise a slow query, I start with EXPLAIN, check whether it uses an index, avoid selecting more columns than needed, paginate with keyset rather than large offsets, and watch for N+1 queries from an ORM. In my projects the SQL I've written was mostly query design and generation in the Rule Builder, plus query use in DuckDB over Parquet, and schema and index design in freelance work.",
 "Joins, indexes, transactions, and how to debug a slow query.", "B/D", "Overclaiming DBA-level tuning.", "I haven't tuned Oracle in production, but my approach is…", ["joins", "indexes", "transactions", "EXPLAIN", "N+1", "pagination"], [{ q: "What is an index and its downside?", a: "It's a data structure, usually a B-tree, that makes lookups fast. The downside is extra storage and slower writes, because the index must be updated too." }]),
K(45, "Microservices split a system into small services that each own a capability and data, and communicate over APIs or events. The benefit is independent deployment and scaling; the cost is operational complexity, distributed failures and data consistency. Our platform used serverless services in that spirit.",
 "The idea is to decompose by business capability, with each service owning its data. It helps teams deploy independently and scale hot spots. The costs are real, though: network failures, distributed tracing, versioned contracts, eventual consistency, and more infrastructure to run. My exposure is through the EDR platform, which had separate stacks for agents and the console, and Lambda-based services with events between them. I'd say don't start with microservices unless the team and problem justify it; a well-structured modular monolith is often a better first step.",
 "Small independent services with clear boundaries; costs are operations and consistency.", "D/C", "Claiming you designed the microservice architecture.", "N/A", ["services", "independent", "eventual consistency", "tracing", "trade-off"], []),
K(47, "The static app is built with Vite, uploaded to a private S3 bucket, and served through CloudFront. CloudFront gives edge caching and HTTPS, and we invalidate the cache on deploy. Hashed asset names let us cache aggressively.",
 "The pipeline builds the app, syncs the output to an S3 bucket, and invalidates CloudFront. The bucket stays private, with CloudFront accessing it through origin access control, so users only reach content through the CDN. Asset filenames include a content hash, so they can be cached for a long time, and index.html has a short cache time so new deploys appear quickly. For rollback, we can redeploy the previous build and invalidate. What I personally handled was the frontend build and deploy; the infrastructure setup was shared with DevOps.",
 "Private bucket, CDN in front, hashed assets.", "A/C", "Direct public S3 hosting.", "N/A", ["S3", "CloudFront", "invalidation", "hashed", "private"], []),
K(55, "I'd start by clarifying requirements and constraints, then define the API and data model, sketch the high-level architecture, and deep-dive into the riskiest parts, such as scaling, failure handling, security and observability. I'd think aloud and check in with the interviewer as I go.",
 "My approach has a fixed order. First, requirements: functional, non-functional, scale and who the users are. Second, capacity estimates, so I know if I need caching or partitioning. Third, the API and the data model. Fourth, a high-level design with the main components. Fifth, a deep dive on the hardest part, often the data flow or scaling. Then failure handling, like retries, idempotency and dead-letter queues. Then observability: logs, metrics, traces, alerts. Then security and multi-tenancy. And I finish with trade-offs and what I'd revisit. On AWS I'd naturally start with managed services, such as API Gateway or AppSync, Lambda or containers, DynamoDB or a relational database depending on the access patterns, and SNS or SQS for decoupling.",
 "Requirements, API, data, design, deep dive, failure, observability, security.", "D/A", "Jumping straight to technologies.", "N/A", ["requirements", "API", "data model", "scaling", "failure", "observability", "security"], []),
K(59, "My approach to production issues is a fixed method: reproduce or scope it, find what changed, follow the request through each layer with logs and dashboards, form a hypothesis, verify it, fix it, and then add a guard, such as an alert or a playbook step, so it's less likely to recur.",
 "Here's the method I follow. First, understand the impact: who's affected, since when, and how bad. Second, check what changed: a deploy, a config change, upstream data. Third, follow one failing request across the layers: the browser's network tab, the API response, the Lambda logs, and the data. I use a correlation ID or request ID to connect them. Fourth, form one hypothesis at a time, and test it rather than changing several things. Fifth, fix or mitigate, and confirm with the same evidence. Sixth, prevent recurrence with a test, an alert, or an update to the playbook. At BluSapphire and at Uber, I resolved known failures using defined playbooks and debugged new ones from logs and dashboards.",
 "Scope, what changed, trace across layers, one hypothesis at a time, prevent.", "A", "Telling an invented story you can't defend.", "N/A", ["impact", "logs", "hypothesis", "root cause", "prevent", "playbook"],
 [{ q: "Rollback or fix-forward?", a: "If the impact is high and the last release is the likely cause, roll back first, since it's the fastest way to restore service. Then debug calmly. Fix-forward only when the fix is small, well-understood and safer than reverting, such as with data migrations that can't be rolled back." }], "C-DDBSTREAM"),
K(66, "The pipeline runs on every pull request: lint, type-check, tests, and build. Merges to main deploy to staging, and then to production after approval. For the frontend, the last step is syncing to S3 and invalidating CloudFront.",
 "A good CI/CD pipeline gives quick feedback and safe releases. On every pull request: install, lint, type-check, run unit tests, and build. On merge: deploy to a staging environment, run smoke tests, then promote to production, with an approval step where needed. I'd add dependency and security scanning, and for backend services, a container build and image scan. For deployment strategies: rolling updates replace instances gradually; blue-green switches traffic between two environments and makes rollback instant; canary sends a small share of traffic first. My hands-on experience is with GitHub and Azure DevOps pipelines and the frontend deploy; DevOps owned the pipeline infrastructure.",
 "Automate lint, test, build, deploy, and roll back safely.", "C", "Claiming Jenkins ownership.", "I haven't set up Jenkins myself, but the concepts map to…", ["lint", "test", "build", "deploy", "rollback", "blue-green", "canary"], []),
K(69, "I follow the test pyramid: many fast unit tests for logic and components, fewer integration tests around API contracts, and a small number of end-to-end tests for critical journeys. Tests run in CI on every pull request.",
 "For me, the priority is testing behaviour that could break users or contracts. Unit tests cover validators, generators and hooks; for example, in Rule Builder, the code that turns conditions into JSON and SQL is easy to test with known inputs and expected outputs. Integration tests check that the UI and API contract line up; contract tests are especially useful when frontend and backend teams are separate. A few end-to-end tests cover the critical flows. I'd also add authorisation tests for tenant isolation. In CI, failures block the merge.",
 "Pyramid: many unit, some integration, few end-to-end.", "A", "Naming tools you haven't used.", "N/A", ["unit", "integration", "contract", "end-to-end", "CI"], []),
K(43, "Authentication says who you are; authorisation says what you can do. The API validates the token on every request, applies least privilege, and scopes data by organisation. Client-side checks are only a convenience.",
 "There are several layers. Transport: HTTPS everywhere. Authentication: the client sends a token, and an authorizer or filter validates its signature and expiry. Authorisation: the API checks roles and organisation on every request, since hiding a button in the UI isn't security. Data protection: least-privilege IAM, encryption at rest, and input validation. Also: CORS configured narrowly, no secrets in the frontend, and rate limiting. In our systems, I integrated the login and token flow and enforced organisation scoping in the console, while the identity and authorizer setup was platform-owned.",
 "Validate identity, check permissions on the server, scope by tenant.", "C", "Saying UI hiding is authorization.", "N/A", ["authentication", "authorisation", "JWT", "least privilege", "server-side"], [{ q: "JWT vs sessions?", a: "JWTs are stateless, so services can validate them without a session store, which suits APIs. The downside is revocation: you rely on short expiry and refresh rotation. Sessions are easy to revoke but need a shared store." }], "C-AUTH"),
K(91, "DynamoDB is a managed key-value and document database that scales horizontally with single-digit-millisecond access. In a single-table design, you model your access patterns first and encode entity types in the keys. It's great for known patterns, but not for ad-hoc queries or joins.",
 "With DynamoDB you design for your queries. On the EDR platform, keys looked like AGENT##org for the partition and the endpoint ID for the sort key, so fetching all agents for an organisation is one query. Every access pattern is a GetItem or a Query, never a scan. The trade-offs: no joins or ad-hoc filtering; a new access pattern may need a new index or a redesign; you must avoid hot partitions; and global secondary indexes are eventually consistent. For reporting and investigation, we used S3 with Parquet and SQL instead. I'd be clear that the table design was owned by the backend team; I understand the access patterns and consume the results.",
 "Design keys around the questions you'll ask.", "E/D", "Saying DynamoDB is faster than SQL.", "The backend team owned that design; here's how I understand it…", ["partition key", "sort key", "access pattern", "GSI", "hot partition", "no joins"],
 [{ q: "Why not PostgreSQL?", a: "PostgreSQL would also work. We preferred DynamoDB because the access patterns were known and key-based, org-partitioned, with serverless scaling and low operations. If we needed ad-hoc queries, joins, or transactions across entities, I'd use Postgres." }, { q: "What if traffic became 10×?", a: "DynamoDB scales with partitions, so I'd check for hot partitions, capacity mode, and throttling. I'd also review caching for repeated reads, and make sure Lambda concurrency and downstream limits keep up." }]),
K(122, "WebSockets keep a persistent connection so the server can push data. Polling repeatedly asks for updates. We used AppSync subscriptions for async results, and 30-second polling for the agent list. I chose per data type: push where timing matters, poll where it's simpler and good enough.",
 "Polling is simple and robust: the client asks every few seconds. The cost is wasted calls and added latency. A WebSocket keeps one connection open and pushes messages when they're ready, which is ideal for results of long-running queries. Server-Sent Events are a middle option: one-way streaming over HTTP. In our console, subscriptions delivered asynchronous investigation results, while the agent list refreshed by polling every thirty seconds, since a small delay was acceptable there. With WebSockets you need to handle reconnects, authentication at connect time, and missed messages, for example by re-querying by ID on reconnect.",
 "Push when timing matters; poll when simple is enough.", "A/C", "Claiming a custom WebSocket manager.", "N/A", ["push", "polling", "reconnect", "SSE", "trade-off"], [{ q: "What happens if the WebSocket drops?", a: "The client reconnects with backoff and resubscribes. Because messages can be missed during the gap, we can re-query the current state by request ID so nothing is lost." }], "C-RT"),
];
